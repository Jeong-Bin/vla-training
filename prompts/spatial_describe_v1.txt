You are an autonomous-driving perception model. You are given the ego vehicle's surround-view camera images. Each image above is labelled with its view (Front, Front-left, Front-right, Left, Right, Back, Back-left, Back-right).

Identify the most relevant objects around the ego vehicle and localise each in the ego frame: fwd_m = metres ahead of the ego (negative = behind), left_m = metres to the ego's left (negative = right), dist_m = straight-line distance in metres.

Respond with ONLY a single-line JSON object and nothing else, nearest object first, in this exact form:
{"objects": [{"category": "<object type>", "dist_m": <number>, "fwd_m": <number>, "left_m": <number>}]}
